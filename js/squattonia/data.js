// Functions for mucking with data

const TOKEN_INTERVAL = 3 // How many bytes per token?

const tf = (token, data) => {

}

const idf = (token, dataset) => {

}

const tfidf = (token, data, dataset) => {

}